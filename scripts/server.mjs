import {createServer} from 'node:http';import {execFile} from 'node:child_process';import {readFile} from 'node:fs/promises';import {extname,join,normalize} from 'node:path';
const root=process.cwd(),mime={'.html':'text/html; charset=utf-8','.json':'application/json; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8'};
createServer(async(req,res)=>{if(req.url==='/refresh'&&req.method==='POST'){execFile(process.execPath,['scripts/build-data.mjs'],{cwd:root,env:{...process.env,LIVE:'1'}},e=>{res.writeHead(e?500:204);res.end()});return}const rel=req.url?.split('?')[0]||'/web/';const file=normalize(join(root,rel==='/'?'/web/index.html':rel.endsWith('/')?`${rel}index.html`:rel));if(!file.startsWith(root)){res.writeHead(403);return res.end()}try{const data=await readFile(file);res.writeHead(200,{'content-type':mime[extname(file)]||'application/octet-stream'});res.end(data)}catch{res.writeHead(404);res.end('Not found')}}).listen(4173,()=>console.log('Mac Price Radar: http://localhost:4173/web/'));
